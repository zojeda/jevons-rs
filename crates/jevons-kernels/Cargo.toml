[package]
name = "jevons-kernels"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
jevons-formats.workspace = true
half = "2.7"
bytemuck = "1"
cubecl = { version = "=0.11.0-pre.4", features = ["hip"] }
# hiprtc C++ code generation (the default LLVM path rejects some kernels).
cubecl-hip = { version = "=0.11.0-pre.4", default-features = false, features = ["cpp"] }

[lints]
workspace = true
